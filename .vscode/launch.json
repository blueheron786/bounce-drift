{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build GBA ROM",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/build_completed.js",
            "preLaunchTask": "build",
            "console": "integratedTerminal",
            "skipFiles": ["<node_internals>/**"],
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}
